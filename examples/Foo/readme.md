## Adding your own physics method and code to HiQGA
The files in this directory need to be included in `src/GeophysOperator.jl` if you wish to contribute to HiQGA .jl. Alternatively, look at the example in `examples/Bar` for a way to add your physics without modifying HiQGA.jl
